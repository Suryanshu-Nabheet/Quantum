# Workspace ignore and walking utilities

This directory no longer contains semantic retrieval/indexer code. The remaining files are shared workspace utilities used by context providers, tools, rules loading, and autocomplete safety checks.

Keep this code focused on:

- `.gitignore` / `.agentignore` handling
- default security ignores
- bounded workspace directory walking
- shared file traversal cache invalidation
