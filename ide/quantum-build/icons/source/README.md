# Deprecated

Do not add logos here. Branding masters live in **`assets/`**:

- `Quantum.png` / `Quantum-dark.png` — light/dark transparent marks
- `app-icon.png` / `app-icon-light.png` — app icon marks
- `lockup-*.png` — horizontal wordmark lockups

Run `./icons/generate_quantum_assets.sh` (or build — it runs automatically via `prepare_icons.sh`).
